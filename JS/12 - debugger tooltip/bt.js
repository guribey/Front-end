// BREAKPOINT ve TOOLTIP

/* tooltip ayar ipucu
kod yazarken fonksiyonun üstüne geldiğinde
fonksiyonun ne olduğunu anlatmak için açılan
 ufak pencere. */

let g ="20"
parseInt(g) //mouseu parseInt in üstüne getir. o tooltip işte.






/* BREAKPINT kodu kısım kısım çalıştırmaya yarıyo.
mesela uzun bi kodda hatayı nerede yaptığını
bulmak için kullanabilirsin. 

bunu yapmak için ayırmak istediğin yere debugger yazıyosun.
console'a değil yanındaki suorces kısmına geçiyosun.
oradan adım adım ilerleyerek kodu inceleyebiliyosun.
*/


debugger

let a = 10
let b = 15
let c = a+b
let d = a+b+c
let e = b+c+d

let sonuc = a+b+c+d+e

console.log(sonuc)

